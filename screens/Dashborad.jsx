import { SafeAreaView } from 'react-native';
import { Text } from 'react-native';

export const Dashborad = () => {
  return (
    <SafeAreaView>
      <Text>Dashborad</Text>
    </SafeAreaView>
  );
};
